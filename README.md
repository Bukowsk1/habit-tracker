# Habit Tracker

Проект трекера привычек с веб-интерфейсом и REST API. Позволяет создавать, отслеживать и анализировать выполнение ежедневных привычек с подробной статистикой.

## Возможности

### Основной функционал
- ✅ Создание и управление привычками
- ✅ Отметка выполнения привычек за текущий день
- ✅ Просмотр детальной информации о каждой привычке
- ✅ Расчет текущего стрика (серии последовательных дней)
- ✅ Подробная статистика по каждой привычке
- ✅ Современный веб-интерфейс

### Статистика и аналитика
- 📊 Общее количество выполнений
- 🔥 Текущий и максимальный стрик
- 📈 Процент успешных дней
- 📅 Последние даты выполнения
- 📋 Сводная страница статистики всех привычек

### API
- 🔌 Полноценный REST API
- 📝 Автоматическая документация Swagger UI
- 🔒 Централизованная обработка ошибок
- 📊 JSON-статистика для каждой привычки

## 🏗️ Архитектура

Проект построен по принципам чистой архитектуры:

```
habit_tracker/
├── api/           # REST API эндпоинты
├── core/          # Бизнес-логика и модели
│   ├── exceptions.py    # Кастомные HTTP-исключения
│   ├── models.py        # Pydantic модели данных
│   └── services.py      # Сервисы и статистика
├── views/         # Веб-маршруты и контроллеры
├── templates/     # HTML-шаблоны (Jinja2)
├── static/        # CSS и JavaScript
└── main.py        # Точка входа приложения
```

## 🛠️ Установка и запуск

### Требования
- Python 3.8+
- pip

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск сервера
```bash
python -m habit_tracker.main
uvicorn habit_tracker.main:app --reload
```

Сервер будет доступен по адресу: http://localhost:8000

## 📖 Использование

### Веб-интерфейс

#### Главная страница (`/`)
- Просмотр списка всех привычек
- Добавление новой привычки
- Отметка выполнения привычек
- Переход к статистике

#### Страница привычки (`/habit/{id}/`)
- Детальная информация о привычке
- Редактирование названия
- Удаление привычки
- Просмотр текущего стрика

#### Страница статистики (`/stats/`)
- Сводная статистика по всем привычкам
- Метрики: общее количество, стрикт, процент успеха
- Последние даты выполнения

### REST API

API доступен по префиксу `/api/habits` и предоставляет следующие эндпоинты:

#### Управление привычками
- `POST /api/habits/` - Создать привычку
- `GET /api/habits/` - Получить все привычки
- `GET /api/habits/{id}/` - Получить конкретную привычку
- `PUT /api/habits/{id}/` - Обновить привычку
- `DELETE /api/habits/{id}/` - Удалить привычку

#### Отметка выполнения
- `POST /api/habits/{id}/mark/` - Отметить выполнение за сегодня

#### Статистика
- `GET /api/habits/{id}/stats/` - Получить статистику привычки

### Примеры API-запросов

#### Создание привычки
```bash
curl -X POST "http://localhost:8000/api/habits/" \
  -H "Content-Type: application/json" \
  -d '{"name": "Утренний бег"}'
```

#### Отметка выполнения
```bash
curl -X POST "http://localhost:8000/api/habits/1/mark/"
```

#### Получение статистики
```bash
curl "http://localhost:8000/api/habits/1/stats/"
```

**Ответ:**
```json
{
  "id": 1,
  "name": "Утренний бег",
  "total_marks": 12,
  "current_streak": 5,
  "max_streak": 7,
  "success_rate": 83.33,
  "last_dates": [
    "2025-07-18",
    "2025-07-17",
    "2025-07-16",
    "2025-07-15",
    "2025-07-14"
  ]
}
```

## 📊 Статистика

### Метрики привычек
- **Общее количество выполнений** - сколько раз была отмечена привычка
- **Текущий стрик** - количество последовательных дней выполнения
- **Максимальный стрик** - самая длинная серия последовательных дней
- **Процент успешных дней** - отношение выполнений к дням с момента первой отметки
- **Последние даты** - 5 последних дат выполнения

## 🛡️ Обработка ошибок

Проект использует централизованную обработку ошибок через кастомные HTTP-исключения:

- `HabitNotFoundException` (404) - привычка не найдена
- `HabitAlreadyMarkedTodayException` (409) - привычка уже отмечена сегодня
- `HabitNameConflictException` (400) - дубликат имени привычки
- `InvalidInputException` (400) - некорректный ввод

Exception handlers автоматически адаптируют ответы в зависимости от типа запроса:
- **API-запросы**: возвращают JSON с соответствующим HTTP-кодом
- **Веб-запросы**: перенаправляют на соответствующую страницу с сообщением об ошибке

## 📚 Документация API

Автоматическая интерактивная документация доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🧪 Тестирование

Проект разработан с учетом автотестирования. Структура файлов строго соответствует требованиям для прохождения тестов.

## 📝 Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **Pydantic** - валидация данных и сериализация
- **Jinja2** - шаблонизатор для веб-интерфейса
- **Uvicorn** - ASGI сервер
- **Python 3.8+** - версия Python


## 📄 Лицензия

Этот проект является учебным и не имеет лицензии.